# Risks: Veil Ecosystem

## Risk Register

Machine-readable risk state lives in `.hekton/risk-register.yaml`. Keep this
Markdown file as the human-readable explanation of material risks and mitigations.

| ID | Date | Risk | Impact | Likelihood | Mitigation | Status |
|---|---|---|---|---|---|---|
| RISK-0001 | 2026-08-24 | Initial governance baseline needs first human/agent review | Medium | Medium | Run governance preflight and end-session review during the first material session | Open |
| RISK-0002 | 2026-08-24 | This document goes stale silently — no CI or process ties it to the 6 (was 5) component repos' actual state | High | High | Treat `docs/architecture.md`'s Integration Status table as a required update whenever a cross-repo integration lands in any component repo; no automated check enforces this yet. **Materialised same-day:** a doubt-driven-development review found the first draft was already wrong in 3 load-bearing places, sourced from a 2-day-old audit never re-checked against live repo state — see `docs/decisions.md`. **Materialised again, 2026-09-04, a sharper case:** the Integration Status diagram/table showed `veil-proxy → veil-custodian` for the enrolment call for the document's entire first ten days — architecturally impossible under ADR-D/ADR-N (a device may never call the custodian directly) since before the document's own first draft. No review cycle caught it because no operator-authority component existed yet whose absence would have made the wrong edge obvious; it took veil-enrol actually being built to notice. Corrected twice now; the underlying no-CI gap remains open both times. **First automated mitigation landed 2026-09-04**: `docs/interactive-plan.md`'s Phase 0 contradiction checker (`scripts/eco_checker.py`) now runs several of these exact checks (link resolution, risk-register parity, repo-name-vs-`gh api`, pin currency, privacy-vs-visibility) locally via `scripts/eco.sh status` / `verify-project.sh` — not yet in CI (deferred, protected-path commit needed), so this remains a partial, not full, mitigation. | Open |
| RISK-0003 | 2026-08-24 | No cryptographic *signing* exists in veil-proxy's pipeline, so veil-observatory's real verifier has nothing genuine to check yet | High | Low (closed for `edge_event.v1`) | **Closed 2026-08-30:** signing, the emitter, and the audit-sink wiring all landed and merged; a genuine cross-process run (real signed record, real separate `veil-observatory serve` process, `202 Accepted`, persisted with a real HMAC pseudonym) confirmed the whole chain works. `Receipt`/`veil.receipt.v2` signing remains unbuilt — deliberately out of scope pending the aggregator, tracked separately. | Closed for `edge_event.v1`; `Receipt` scope tracked in next-actions.md |
| RISK-0004 | 2026-08-24 | Demask authorisation (`--actor`/`--role`) is self-asserted, not authenticated, ecosystem-wide. **Disambiguation, 2026-09-12:** this ID is unrelated to veilgremlin's own `RISK-0004` (a false-positive-detector-rate risk in that repo's own `docs/risks.md`) — the two registries number independently. Refer to this one as **F4** or **VE-RISK-0004** in any cross-referencing text to avoid the collision. | High | Medium | **Named as a beta-gating decision 2026-09-12 (D-BETA-1, `docs/beta-implementation-plan.md` §3):** verdict is partially beta-blocking — mitigate and disclose rather than attempt real authentication, which needs OS-level caller attestation (a separate research track, not a beta task). Ship three mechanical mitigations (hooks refuse to spawn `vg demask` from inside a wrapped session; restrictive file permissions on packs/state dir; a test asserting the vault key never enters the wrapped environment) plus a written, signed threat-model disclosure in the beta participation agreement. No longer "not started." | Open, tracked upstream in veilgremlin |
| RISK-0005 | 2026-08-24 | A component repo's own documentation can itself be stale relative to its code, and this document has no way to detect that except a fresh doubt-driven review | Medium | Medium | Materialised in the 3rd review cycle: veil-observatory's `correlation/correlator.py` and `ADR-0016` both still assert a "structural stub" signature verifier a real implementation had already superseded — this document copied that claim faithfully from a stale source, not a stale audit this time. Mitigation is procedural, not automatable yet: verify against code, not against any repo's prose about itself, including this one's. | Open |
| RISK-0006 | 2026-08-30 | veil-demo's live deployment is down (Fly.io trial ended, no credit card on file) — the ecosystem's oldest "real" integration is currently non-functional in production. **Disambiguation, 2026-09-13:** distinct from `RISK-0007` (hosting/billing for the beta's own, separate custodian/observatory deployment) — do not conflate them. | Medium | Certain (already materialised) | Human decision needed: add a credit card and restore the deployment, or accept the demo stays offline. Independent of any code defect — confirmed via a failed TLS handshake and `fly status`'s own error message. | Open, tracked in veil-ecosystem/docs/next-actions.md |
| RISK-0007 | 2026-09-13 | No hosting/billing target had been decided for the beta's own custodian/observatory deployment. **Disambiguation:** distinct from `RISK-0006` (veil-demo's own dead deployment) even though both involve Fly.io and a missing card. | Medium | High | **Closed same day, 2026-09-13:** recorded as D-BETA-6 (part 2) in `docs/decisions.md` — Fly.io, with a new credit card on file, is the hosting/billing target for the beta's own custodian/observatory deployment. Actually standing up that deployment (Phase B) and adding the card are still real follow-on work. | Closed |
| RISK-0008 | 2026-09-14 | The real `claude` CLI redacts its own internal billing header (`x-anthropic-billing-header` → `[REDACTED:SECRET]`) whenever `ANTHROPIC_BASE_URL` points away from the default Anthropic endpoint, and the real Anthropic API rejects that redacted value outright — breaking `ANTHROPIC_BASE_URL` redirection, the exact mechanism beta condition 1 (§1) and this plan's own M5/A2 framing depend on. Not a code defect in any component repo — confirmed by veilgremlin's own live-run proof (`scripts/a2-live-proof.sh`, `INT-2026-09-13-001`) that vg-proxy correctly forwards whatever the real CLI sends, and that the same prompt succeeds with `ANTHROPIC_BASE_URL` unset. | Critical | Certain (confirmed reproducible) | Filed jointly with veilgremlin's own equivalent entry (linked below; disambiguated by repo per this registry's own RISK-0004/RISK-0006 precedent — do not conflate the two). A bounded check of `claude gateway` (a separate enterprise auth/telemetry subcommand, its own config) did not reveal a documented bypass. Needs a real owner and product-level decision: a trusted-local-proxy opt-in this investigation missed, `claude gateway` as the real intended mechanism despite its heavier shape, or a different proof path for beta condition 1 entirely. Beta-blocking until resolved. | Open, tracked jointly in veilgremlin/docs/risks.md#risk-0014 |
